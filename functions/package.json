{
  "name": "frugalgpt-functions",
  "private": true,
  "main": "index.router.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "echo \"No function lint configured\"",
    "test": "node --test",
    "serve": "firebase emulators:start --only functions",
    "deploy": "cp ../models.json ./models.json && firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.92.0",
    "@google/genai": "^1.26.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^7.2.5",
    "openai": "^4.104.0"
  }
}
